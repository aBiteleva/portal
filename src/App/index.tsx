import React from 'react';
import MainPage from '../MainPage';
import './styles.module.scss';

const App = (props: any) => {
    return (
        <MainPage {...props} />
    );
};

export default App;
